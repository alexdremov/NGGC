def giveYouUp() {
	let x = 0;
	let userData = 100000;
	print coub(1);
	let something = 10 * coub(1) * 100  * coub(1) * coub(2);
	print x;
	print userData;
	print something;
}

def coub(z) {
	let x = z;
	let t = z;
	let r = z;
    ret x * t * r;
}

def poly(a) {
    print a;
}

def poly(a, b) {
    print a * b;
}
