def giveYouUp() {
	let x = 0;
	let userData = 100000;
	print(fact(20));
	while(x < userData) {
	    fact(20);
	    x += 1;
	}
}

def fact(z) {
    if (z <= 1){
        ret 1;
    }
    ret fact(z - 1) * z;
}

def poly(a) {
    print a;
}

def poly(a, b) {
    print a * b;
}
