def giveYouUp() {
	let x = 0;
	let userData = 100000;
	let res = 0;
	while(x < userData) {
	    res = fact(20);
	    x += 1;
	}
	print res;
}

def fact(x) {
    if (x <= 1){
        ret 1;
    }
    ret fact(x - 1) * x;
}
